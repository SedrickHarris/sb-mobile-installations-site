# SB Mobile Installations Cloudflare Pages Deployment Plan

**Document:** `17-cloudflare-deployment.md`  
**Project:** SB Mobile Installations Website and Installer Recruitment Project  
**Business:** SB Mobile Installations, LLC  
**Repository:** https://github.com/SedrickHarris/sb-mobile-installations-site.git  
**Hosting target:** Cloudflare Pages  
**Application output:** Next.js static export  
**Production domain:** `https://sbmobileinstallations.com/` pending final DNS and canonical-host approval  
**Production branch:** `main`  
**Status:** Deployment and operations draft  
**Version:** 0.1  
**Last updated:** September 9, 2026

---

## 1. Purpose

This document defines how the SB Mobile Installations website will be built, previewed, validated, deployed, connected to its production domain, monitored, and rolled back on Cloudflare Pages.

It governs:

- Cloudflare account and project ownership;
- GitHub integration;
- production and preview branch behavior;
- build settings and environment variables;
- custom domains, DNS, TLS, and canonical hosts;
- redirect and response-header deployment;
- preview indexation and access controls;
- static asset and cache behavior;
- form-backend boundaries;
- analytics and monitoring activation;
- launch and migration procedures;
- deployment verification;
- rollback and incident response; and
- operational access and maintenance.

This plan implements the static-export architecture in `16-technical-architecture.md`. It does not authorize publishing unverified business information, job terms, services, locations, or entity relationships.

---

## 2. Deployment Outcome

The deployment system is successful when:

- approved commits produce reproducible static builds;
- every pull request or approved working branch can receive a safe preview;
- only `main` deploys to the production environment;
- the canonical domain serves the intended release over HTTPS;
- the alternate host and verified legacy URLs redirect correctly;
- preview domains remain out of search indexes;
- forms use the correct environment and never expose secrets;
- production analytics does not collect preview traffic;
- failed builds do not replace the working production deployment;
- a known-good release can be restored quickly; and
- SB Mobile Installations retains durable control of the Cloudflare account, domain, DNS, and production project.

---

## 3. Hosting Model

### Approved baseline

Use Cloudflare Pages to host the `out` directory generated by a Next.js static export.

```text
GitHub repository
    -> Cloudflare Pages build
    -> npm clean install
    -> project validation
    -> next build
    -> out/ static artifact
    -> Cloudflare global edge delivery
```

### Trusted backend boundary

Cloudflare Pages serves public HTML, CSS, JavaScript, images, fonts, sitemaps, robots directives, headers, and redirects. It must not expose or perform secret-dependent operations in browser code.

Use a separately configured Cloudflare Worker, a reviewed Pages Function, an approved ATS form, or another approved server-side integration for:

- application submissions;
- installer-network registrations;
- candidate referrals;
- commercial inquiries;
- Turnstile server validation;
- ATS and CRM credentials;
- resume or file transfer;
- signed webhook requests;
- server-confirmed conversion events; and
- durable queues or retries.

The exact form and integration platform remains a project decision blocker.

---

## 4. Official Platform Baseline

Current Cloudflare documentation supports deploying a Next.js static export to Pages. The current framework preset uses:

| Setting | Cloudflare static Next.js value |
|---|---|
| Framework preset | Next.js Static HTML Export |
| Build command | `npx next build` or the equivalent project script |
| Build output directory | `out` |
| Production branch | Project-selected branch, recommended `main` |

Cloudflare Pages Git integration can deploy on pushes, create preview deployments for approved branches and pull requests, and report build status to GitHub.

Platform features and limits change. Verify current official documentation before initial setup, domain cutover, or a material framework upgrade.

---

## 5. Account Ownership

### Required ownership model

- The production Cloudflare account must be controlled by SB Mobile Installations or another explicitly approved business owner.
- The DNS zone and Pages project should be in the same approved business account where practical.
- Sirius Systems Digital Marketing receives the minimum role required to configure and maintain the project.
- Individual developer accounts must not be the only production administrators.
- At least two business-authorized administrators should exist when available.
- Multifactor authentication is required.
- Recovery methods must use business-controlled channels.
- Shared credentials are prohibited.

### Required ownership record

Document outside the public repository:

- Cloudflare account name and ID;
- zone name and ID;
- Pages project name;
- production domain;
- billing owner;
- primary and backup administrators;
- technical maintainer;
- registrar;
- nameserver status;
- recovery owner;
- emergency contact; and
- approved vendor access and review date.

Do not place authentication tokens, account recovery codes, or sensitive IDs in this document or Git.

---

## 6. Recommended Cloudflare Resource Names

Use stable, descriptive names.

| Resource | Recommended name |
|---|---|
| Pages project | `sb-mobile-installations-site` |
| Production branch | `main` |
| Custom domain | `sbmobileinstallations.com` or approved canonical host |
| Preview environment | Cloudflare-generated branch and commit deployments |
| Form Worker | `sb-mobile-installations-forms` if an owned backend is selected |
| Turnstile widget | `sb-mobile-installations-public-forms` |
| Analytics site | Canonical production domain |

Resource names are operational identifiers. They do not resolve the public relationship between SB Mobile Installations, LLC and Doral Transport LLC.

---

## 7. Prerequisites

Before creating the production Pages project, confirm:

- access to the GitHub repository;
- repository default and production branch;
- Cloudflare account ownership;
- domain registrar access;
- current DNS records and nameservers;
- current website host and rollback path;
- current email-related DNS records;
- approved canonical host;
- Node.js version;
- package manager and lockfile;
- successful local static build;
- expected `out` directory contents;
- production and preview environment values;
- form and ATS/CRM architecture;
- analytics and consent decisions;
- legacy URL inventory;
- approved launch window; and
- named technical and business launch owners.

Do not change nameservers or production DNS until the existing zone has been inventoried and reproduced accurately.

---

## 8. GitHub Integration

### Recommended connection

Connect Cloudflare Pages to:

- GitHub account or organization: approved repository owner;
- repository: `SedrickHarris/sb-mobile-installations-site`;
- production branch: `main`; and
- repository access: only the required repository where the GitHub App permissions allow it.

### Permission standard

- Install the Cloudflare Workers and Pages GitHub App with the narrowest repository scope.
- Do not grant all-repository access when only this repository is needed.
- Record who authorized the installation.
- Review the installation quarterly and after any vendor change.
- Removing GitHub access stops future Git-connected builds but does not automatically remove prior deployments; document this distinction during offboarding.

### Expected behavior

- Push to `main`: production build and deployment.
- Push to an allowed non-production branch: preview build.
- Pull request from an allowed branch: unique preview URL and GitHub check status.
- Failed build: no new production deployment.
- Skipped build: no new deployment or check status where Cloudflare documents that behavior.

### Build-skip controls

Cloudflare supports recognized commit-message prefixes that skip a Pages build. Use them sparingly and only for changes that cannot affect the deployed artifact.

Do not skip a build for changes to:

- application code;
- content or jobs;
- public assets;
- dependencies;
- configuration;
- redirects;
- headers;
- search files;
- schema;
- environment expectations; or
- deployment documentation that accompanies an immediate operational change.

---

## 9. Branch Deployment Controls

### Recommended policy

| Branch type | Cloudflare build | Environment | Production systems | Indexation |
|---|---:|---|---:|---:|
| `main` | Yes | Production | Yes, after approval | Approved pages only |
| Feature/content branch | Yes | Preview | No by default | Noindex |
| Automated dependency branch | Yes | Preview | No | Noindex |
| Experimental branch | Conditional | Preview/test | No | Noindex |
| Archive branch | No | None | No | None |

### Preview allowlist

Use Cloudflare branch controls to build:

- active pull-request branches;
- approved content review branches;
- approved dependency update branches; and
- explicitly named integration test branches.

Exclude noisy, archived, or automated branches that do not require a preview.

### Production protection

- Only `main` may be selected as the production branch.
- GitHub branch protection and required checks should prevent unreviewed changes from reaching `main`.
- Cloudflare account users must not change the production branch without an approved architecture or incident decision.
- Record production-branch changes in the decision log.

---

## 10. Build Configuration

### Recommended Pages settings

| Setting | Required value |
|---|---|
| Framework preset | Next.js Static HTML Export |
| Production branch | `main` |
| Build command | `npm run build` |
| Build output directory | `out` |
| Root directory | `/` unless the repository becomes a monorepo |
| Package manager | npm unless changed by decision |
| Dependency install | Determined from committed `package-lock.json` |
| Node version | Project-pinned supported version |

### Build command contract

`npm run build` must:

1. exit nonzero on a fatal validation failure;
2. produce a Next.js static export;
3. place deployable files in `out/`;
4. include `_headers` and `_redirects` when used;
5. exclude draft, blocked, and test content;
6. exclude expired active-job schema and sitemap entries;
7. generate correct environment-specific metadata; and
8. avoid network dependencies that make ordinary builds fragile unless explicitly approved.

### Quality-gate approach

Two supported patterns exist:

**Pattern A: validate inside the Pages build**

```text
npm run check
```

Where `check` performs type checking, linting, tests, content validation, schema validation, link validation, and the production build.

**Pattern B: GitHub checks plus Pages build**

- GitHub Actions runs the complete quality suite.
- Cloudflare runs `npm run build`.
- GitHub branch protection requires quality checks before merge.

Pattern B avoids repeating every expensive test in Cloudflare, but Cloudflare must still fail on build-time content and route errors. Select one documented production gate and keep local, GitHub, and Cloudflare Node/dependency versions aligned.

---

## 11. Node and Dependency Configuration

### Required repository controls

- commit `package.json`;
- commit `package-lock.json`;
- add `.nvmrc` or `.node-version`;
- declare a compatible `engines.node` range;
- use a clean lockfile-based install in CI; and
- prevent unreviewed lockfile replacement.

### Cloudflare environment

Set the supported Node version through the approved Cloudflare Pages mechanism when required by the build image. Confirm the actual version in build logs.

### Dependency rules

- Do not run uncontrolled major upgrades during deployment setup.
- Review Next.js, React, Tailwind, TypeScript, ESLint, and build-tool compatibility as a group.
- Remove packages unused by the production build.
- Fail on unresolved high-impact security issues unless an approved exception exists.
- Do not allow install scripts from unnecessary packages.
- Keep local, GitHub Actions, and Cloudflare installation commands consistent.

---

## 12. Environment Model

### Environments

| Environment | Host | Purpose | Real submissions | Production analytics | Indexable |
|---|---|---|---:|---:|---:|
| Local | `localhost` | Development | No | No | No |
| Preview | Cloudflare preview URL | Review and QA | No by default | No | No |
| Production | Canonical domain | Public site | Yes | Yes | Approved URLs |

### Environment detection

Cloudflare Pages provides build variables such as:

- `CF_PAGES`;
- `CF_PAGES_BRANCH`;
- `CF_PAGES_COMMIT_SHA`; and
- `CF_PAGES_URL`.

These can support build identification and diagnostics. They must not override the canonical production-origin policy.

### Required project variables

Potential public build variables:

| Variable | Scope | Purpose |
|---|---|---|
| `NEXT_PUBLIC_SITE_URL` | Production/preview with controlled behavior | Approved canonical production origin for metadata |
| `NEXT_PUBLIC_SITE_ENV` | Each environment | `production`, `preview`, or `local` |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | Production only or empty in preview | GA4 identifier |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | Environment-specific | Public widget key |
| `NEXT_PUBLIC_FORM_ENDPOINT` | Environment-specific | Approved public submission endpoint |

Names may change during implementation. Every `NEXT_PUBLIC_` value is visible to users and must be safe to disclose.

### Canonical URL rule

Production canonicals must always use the approved canonical production origin. Do not use `CF_PAGES_URL` to generate canonical URLs for previews, because that could create environment-specific canonical identities.

Preview behavior should be:

- production canonical references only when explicitly required for content review;
- environment-level `noindex` response control;
- no sitemap submission; and
- no production analytics or form destinations.

---

## 13. Secrets

### Static site rule

The exported site has no safe browser-side secret. Any value bundled into HTML or JavaScript must be treated as public.

### Private values belong elsewhere

Store these only in the trusted Worker, Function, ATS, CRM, or integration environment:

- Turnstile secret key;
- ATS credential;
- CRM credential;
- webhook signing secret;
- email/SMS API key;
- private storage credential;
- server-side analytics secret;
- error-monitoring write token; and
- authentication or encryption key.

### Secret governance

- use Cloudflare encrypted secrets or the approved provider's secret store;
- separate preview and production secrets;
- grant least privilege;
- never echo secrets in build or runtime logs;
- never commit real secrets;
- maintain rotation ownership and schedule;
- rotate immediately after suspected exposure;
- remove vendor access and rotate affected credentials after offboarding; and
- keep `.env.example` limited to names and safe descriptions.

### Build-time private data

Avoid fetching protected candidate, customer, or operational data during a public static build. If a private content API is later introduced, ensure secrets are available only at build time, excluded from output, not logged, and limited to public-approved content.

---

## 14. Pages Project Creation Procedure

1. Sign in to the approved business-controlled Cloudflare account.
2. Open Workers & Pages.
3. Create a Pages application.
4. Select Git integration.
5. Authorize the Cloudflare Workers and Pages GitHub App with access only to the required repository where possible.
6. Select `SedrickHarris/sb-mobile-installations-site`.
7. Set project name to the approved stable project identifier.
8. Select `main` as the production branch.
9. Select Next.js Static HTML Export.
10. Set build command to `npm run build`.
11. Set build output to `out`.
12. Leave root directory at repository root unless the structure changes.
13. Add approved environment variables by environment.
14. Save the configuration.
15. Run the first preview or preproduction build before attaching the public domain.
16. Review build logs and deployed output.
17. Record the project URL, ID, owners, and configuration date in the private operations record.

Do not connect the production domain until the static deployment passes the pre-domain validation checklist.

---

## 15. First Deployment Validation

Verify the initial `*.pages.dev` deployment before domain cutover.

### Build validation

- [ ] Correct repository and commit SHA appear.
- [ ] Correct branch appears.
- [ ] Correct Node version appears.
- [ ] Dependency install uses the lockfile.
- [ ] Validation and build finish successfully.
- [ ] Output directory is `out`.
- [ ] No secrets or PII appear in logs.
- [ ] No unexpected network dependency or warning is ignored.

### Output validation

- [ ] Homepage loads.
- [ ] Careers hub loads.
- [ ] Openings index loads.
- [ ] Approved job pages load.
- [ ] Draft and blocked routes do not exist.
- [ ] Commercial and resource routes load as scoped.
- [ ] 404 behavior works.
- [ ] CSS, fonts, images, scripts, and icons load.
- [ ] `_headers` rules are applied.
- [ ] `_redirects` rules are applied.
- [ ] sitemap and robots files are correct for the preview environment.

### Search validation

- [ ] Preview responses contain the approved noindex control.
- [ ] Preview URLs are not submitted to Google or Bing.
- [ ] Canonical tags do not establish preview URLs as public canonicals.
- [ ] JSON-LD uses approved canonical production URLs only when intentionally reviewed.
- [ ] No test jobs appear as active schema.

### Conversion validation

- [ ] Preview forms use test endpoints or are disabled.
- [ ] No preview application reaches production recruiting systems.
- [ ] No preview inquiry reaches the production commercial pipeline.
- [ ] Production GA4 does not receive preview traffic.
- [ ] Test Turnstile keys and hostnames behave correctly.

---

## 16. Preview Deployment Strategy

### Preview URL types

Cloudflare may provide:

- a unique deployment URL associated with a commit; and
- a branch alias that updates as the branch receives new deployments.

Use the immutable deployment URL for exact release evidence. Use the branch alias for ongoing stakeholder review.

### Preview purpose

Previews support:

- visual review;
- mobile review;
- content approval;
- job page approval;
- accessibility tests;
- metadata and schema inspection;
- link and redirect testing;
- form testing with nonproduction systems; and
- performance smoke tests.

### Preview limitations

- A preview is not public publication approval.
- A preview is not an indexation target.
- A preview must not claim a job is active merely because its template renders.
- A preview must not use production ATS, CRM, email, SMS, storage, or analytics by default.
- A preview may contain unapproved information only when access and handling are appropriate for the review audience.

---

## 17. Preview Indexation Protection

Use layered protection.

### Layer 1: Response header

Apply `X-Robots-Tag: noindex, nofollow` to the Pages project hostname and preview aliases through a verified `_headers` rule or an equivalent Cloudflare configuration.

Example pattern requiring replacement with the actual project hostname and live verification:

```text
https://sb-mobile-installations-site.pages.dev/*
  X-Robots-Tag: noindex, nofollow
```

Branch and deployment host patterns must also be tested. Do not assume one host rule covers every generated preview URL.

### Layer 2: Metadata

Preview builds may also output `noindex, nofollow` metadata as defense in depth. Response headers remain preferable for an environment-wide control.

### Layer 3: Access control

Use Cloudflare Access or another approved restriction for stakeholder previews containing sensitive or unapproved information. Access control protects confidentiality; `noindex` protects discovery. They serve different purposes.

### Layer 4: Operational controls

- do not link previews from public pages;
- do not submit previews to search engines;
- do not use preview URLs in job-board listings, profiles, social bios, citations, or permanent documents;
- remove obsolete branch aliases where appropriate; and
- audit indexed `pages.dev` URLs after launch.

### Production `pages.dev` hostname

The default production Pages hostname can also become discoverable. Either:

- keep it accessible with `noindex` and canonical references to the custom domain; or
- redirect only the production project hostname to the canonical custom domain through a host-capable Cloudflare Redirect Rule.

Do not redirect all wildcard preview hostnames to production, because that would destroy preview behavior and could publish unapproved paths indirectly.

---

## 18. Custom Domain Decision

### Decision required

Choose one canonical host:

**Recommended:** `https://sbmobileinstallations.com/`

Alternate:

`https://www.sbmobileinstallations.com/`

The final choice must consider the current live site, backlinks, Search Console properties, business profiles, email/DNS setup, and existing canonical use.

### Canonical-host behavior

- one host returns the final content;
- the alternate host permanently redirects in one hop;
- all metadata and schema use the canonical host;
- sitemap URLs use the canonical host;
- internal links use canonical paths;
- Google Business Profile, Apple Business Connect, Bing, job platforms, social profiles, and citations use the canonical host; and
- the default production `pages.dev` hostname is noindex or redirected as approved.

---

## 19. DNS Inventory

Before changing the zone, export and verify all records.

### Record categories

- apex `A`, `AAAA`, `CNAME`, or flattening behavior;
- `www`;
- mail `MX`;
- SPF `TXT`;
- DKIM records;
- DMARC;
- domain verification tokens;
- Google and Microsoft verification records;
- Apple or other business verification records;
- subdomains;
- autodiscover or mail-service records;
- CAA;
- existing redirects or proxy hosts;
- third-party services; and
- legacy records that may still carry traffic.

### Migration rule

Do not treat the website's DNS record as the entire zone. Nameserver migration without preserving mail and verification records can interrupt business email, reputation systems, search verification, and other services.

### DNS change log

Record:

- date and UTC time;
- record type;
- name;
- previous value;
- new value;
- TTL;
- proxy status;
- reason;
- person approving;
- person implementing; and
- rollback value.

Do not publish sensitive verification tokens in the repository.

---

## 20. Custom Domain Connection

### When the domain is already on Cloudflare DNS

1. Open the Pages project.
2. Add the approved custom domain.
3. Confirm the suggested DNS change.
4. Verify certificate provisioning.
5. Test HTTPS and host behavior.

### When the domain is not on Cloudflare DNS

The exact path depends on whether the canonical host is the apex or a subdomain and on current Cloudflare requirements.

- Add the domain through the Pages dashboard before manually creating a CNAME.
- For an apex domain, Cloudflare may require the zone to use Cloudflare nameservers.
- For a subdomain, use the Pages-provided target and confirm ownership and certificate validation.
- Do not create a DNS record pointing at Pages without completing the Pages custom-domain association.

### Certificate validation

- Wait for the Pages custom-domain status to become active.
- Verify the full certificate chain.
- Confirm both canonical and alternate hosts behave as designed.
- Check for restrictive CAA records if certificate issuance fails.
- Do not enable a strict transport policy before all required subdomains and HTTPS behavior are understood.

---

## 21. DNS Cutover Plan

### Before cutover

- lower relevant TTLs when operationally useful and safe;
- capture existing DNS and hosting configuration;
- confirm old-site rollback access;
- complete preview QA;
- validate redirects against the legacy URL inventory;
- prepare production environment variables;
- test forms against production systems in a controlled manner;
- confirm analytics and consent configuration;
- freeze conflicting content changes;
- select launch owner and communication channel; and
- schedule a monitored window.

### Cutover

1. Deploy the approved commit to `main`.
2. verify the Pages production deployment.
3. Add or activate the custom domain.
4. Apply the approved DNS change.
5. Confirm certificate and HTTPS.
6. Confirm canonical and alternate-host behavior.
7. Verify priority routes, redirects, assets, headers, forms, analytics, schema, sitemap, and robots.
8. Monitor edge, form, and search systems.

### After cutover

- keep the old host available for rollback during the agreed observation window;
- update Search Console and Bing as needed;
- submit the canonical sitemap;
- update business profiles, recruiting platforms, directories, social profiles, and campaigns;
- monitor 404s and redirect demand;
- confirm email and unrelated DNS services remain operational;
- record the production deployment ID and commit SHA; and
- close the migration freeze after approval.

---

## 22. Redirect Deployment

### Repository location

For static Pages deployment, store the source file at:

```text
public/_redirects
```

Next.js copies it into the final static output. The build must confirm:

```text
out/_redirects
```

exists and matches the approved redirect registry.

### Syntax

Cloudflare Pages uses one rule per line:

```text
[source] [destination] [status]
```

Example:

```text
/contact-us /contact/ 301
/career-opportunities /careers/ 301
```

### Redirect requirements

- use an explicit status code;
- place static rules before dynamic rules;
- keep specific rules before broad patterns;
- avoid chains and loops;
- use one canonical trailing-slash format;
- do not redirect arbitrary missing routes to the homepage;
- do not use redirects to mask thin duplicate pages;
- test every known legacy route; and
- preserve safe campaign parameters only where platform behavior is confirmed.

### Platform limits

Cloudflare currently documents limits for `_redirects`, including separate static and dynamic rule counts. If the migration exceeds those limits, use Bulk Redirects or another approved zone-level mechanism.

### Function routing caveat

Cloudflare documents that `_redirects` rules do not apply to requests handled by Pages Functions. If Functions are added, route exclusions or equivalent redirect logic must be implemented and tested in the Function path.

### Host redirects

The `_redirects` file does not provide the full domain-level redirect behavior needed for every host case. Use Cloudflare Redirect Rules, Bulk Redirects, or another host-aware edge rule for:

- `www` to apex or apex to `www`;
- production `pages.dev` host to the canonical domain; and
- HTTP-to-HTTPS behavior not already enforced by the platform.

---

## 23. Redirect Validation

Automated tests should verify:

- source URL returns the approved redirect code;
- `Location` points directly to the final canonical URL;
- destination returns `200`;
- destination is indexable when intended;
- destination canonical matches itself;
- no loop occurs;
- no chain exceeds one hop where avoidable;
- query handling is safe;
- case variants behave according to policy;
- trailing-slash behavior is consistent; and
- application or form parameters never disclose personal data.

### Required launch set

At minimum test:

- existing homepage;
- `/contact-us`;
- `/career-opportunities`;
- known current job or application URLs;
- HTTP canonical host;
- HTTPS alternate host;
- production Pages hostname;
- missing route; and
- representative static asset.

The final migration inventory must be created from a verified crawl, analytics, Search Console, Bing, backlinks, sitemaps, and stakeholder records before launch.

---

## 24. Response Header Deployment

### Repository location

Store static-response header rules at:

```text
public/_headers
```

Verify the build output contains:

```text
out/_headers
```

### Baseline header set

The following is a starting point, not a copy-and-deploy final policy:

```text
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  X-Frame-Options: DENY

/_next/static/*
  Cache-Control: public, max-age=31536000, immutable

https://sb-mobile-installations-site.pages.dev/*
  X-Robots-Tag: noindex, nofollow
```

### Required review before production

- replace the project hostname with the actual assigned hostname;
- verify branch and deployment preview host coverage;
- confirm whether embedded ATS or approved third-party pages require framing exceptions;
- validate `Permissions-Policy` against actual features;
- add HSTS only after HTTPS and subdomain implications are approved;
- design and test Content Security Policy against actual analytics, Turnstile, fonts, images, forms, and integrations;
- confirm cache rules do not apply to mutable HTML or sensitive responses; and
- test headers on canonical, alternate, preview, asset, and error URLs.

### Function-response caveat

Cloudflare documents that `_headers` rules do not apply to responses generated by Pages Functions. Any Function must set its own security, privacy, CORS, cache, and robots headers.

---

## 25. Content Security Policy Rollout

### Phase 1: dependency inventory

List every required origin for:

- first-party scripts and styles;
- GA4 or tag management;
- Cloudflare Turnstile;
- form endpoint;
- fonts;
- images;
- embedded ATS content;
- consent tooling;
- monitoring; and
- approved media.

### Phase 2: report-only policy

Deploy a reviewed `Content-Security-Policy-Report-Only` policy when a report collector and privacy review exist. Observe violations from real templates and forms.

### Phase 3: enforcement

Remove unnecessary origins and enforce the narrowest working policy.

### CSP rules

- avoid `*` sources;
- avoid `unsafe-eval`;
- minimize or eliminate `unsafe-inline` through framework-compatible nonces, hashes, or validated static behavior where feasible;
- set `object-src 'none'` unless a verified feature requires otherwise;
- set `base-uri 'self'`;
- use `frame-ancestors` for framing control;
- scope `connect-src` to approved analytics and form endpoints;
- test Turnstile and consent flows; and
- update CSP through reviewed code whenever a vendor changes.

Do not deploy a strict CSP blindly and break applications, consent, or analytics on launch day.

---

## 26. HSTS Strategy

### Prerequisites

Before enabling HSTS:

- canonical domain serves valid HTTPS;
- alternate host redirects over HTTPS;
- all required first-party subdomains support HTTPS;
- no legacy service depends on HTTP;
- certificate renewal is understood; and
- business and technical owners approve the recovery implications.

### Rollout

1. Start with a short `max-age`.
2. Monitor domain and subdomain behavior.
3. Increase the duration after confidence is established.
4. Add `includeSubDomains` only after every relevant subdomain is verified.
5. Consider preload only as a separate high-impact decision.

HSTS mistakes persist in browsers until the directive expires. Treat long durations and preload as deliberate security changes, not default launch settings.

---

## 27. Cache Strategy

### Hashed framework assets

Use long-lived immutable caching for fingerprinted assets such as `/_next/static/*`.

### Images and fonts

Use long-lived public caching when filenames are stable or versioned and the asset replacement process is understood.

### HTML

Use Cloudflare Pages defaults unless measured behavior supports a custom policy. HTML must update promptly after a deployment, especially for active and closed jobs.

### Search files

Sitemaps and robots files must refresh with deployments. Do not apply an immutable one-year policy.

### Form and API responses

Sensitive, user-specific, and form-submission responses require explicit no-store behavior in the backend.

### Query-string and cache rules

Do not create broad cache rules that vary on or ignore unknown query strings without testing campaign attribution, preview tools, and form behavior.

### Purges

- rely on atomic Pages deployments for ordinary releases;
- use purge only for verified stale content or incidents;
- avoid broad purge operations without a clear target and validation plan; and
- record manual purge actions during incidents.

---

## 28. Static Asset Deployment

### Requirements

- optimize images before commit;
- use descriptive, stable filenames;
- include explicit image dimensions;
- use WOFF2 fonts with verified licenses;
- avoid oversized source files in the public bundle;
- validate missing-asset references;
- keep source design files outside the production asset path unless intended for public download;
- do not include resumes, applicant documents, internal reports, or client-confidential media in `public/`; and
- inspect `out/` for accidental files before launch.

### Artifact inspection

The deployment check should report:

- total output size;
- largest assets;
- JavaScript by route or bundle;
- uncompressed images;
- duplicate assets;
- source maps when enabled;
- unexpected file extensions;
- public configuration files; and
- prohibited filenames or directories.

---

## 29. Forms and Cloudflare Deployment

### Static-site behavior

The Pages deployment may render and submit public forms, but it cannot safely hold ATS/CRM secrets in exported code.

### Recommended domain pattern

Potential backend origins include:

- `https://forms.sbmobileinstallations.com/`; or
- a same-domain route proxied to a Worker through an approved zone rule.

The final choice must consider CSP, CORS, cookies, logging, DNS, monitoring, and operational ownership.

### Environment separation

| Site environment | Form destination | Turnstile key | CRM/ATS |
|---|---|---|---|
| Local | local mock/test endpoint | test | sandbox/test |
| Preview | preview endpoint | test or preview | sandbox/test |
| Production | production endpoint | production | production |

### Deployment checks

- endpoint origin is correct;
- browser code contains no private credential;
- Turnstile secret is verified server-side;
- accepted applications create the correct test/production record;
- downstream failure does not show success;
- recruitment and commercial endpoints remain separate;
- analytics conversion fires only after accepted processing;
- logs contain no uncontrolled form body; and
- CORS permits only the required origins and methods.

---

## 30. Turnstile Deployment

### Widget environments

Create or configure separate test and production behavior.

Production hostname allowlist should include only approved public hosts. Preview testing may use Cloudflare test keys or a dedicated preview configuration.

### Required backend behavior

- send each token to Cloudflare Siteverify;
- keep the secret outside Pages static output;
- validate token success;
- validate hostname and action where configured;
- reject expired or replayed tokens;
- enforce server-side form validation independently; and
- record a safe failure category without logging candidate data.

### Launch tests

- normal successful submission;
- invalid token;
- missing token;
- expired token;
- token replay;
- wrong hostname;
- JavaScript blocked;
- slow network;
- downstream ATS/CRM failure; and
- accessibility and keyboard behavior.

Turnstile reduces automated abuse. It does not replace input validation, rate limiting, idempotency, monitoring, or human review.

---

## 31. Analytics Activation

Follow `15-analytics-and-measurement.md`.

### Production requirements

- GA4 measurement ID set only in the production environment unless a separate test property is used;
- analytics gated to the canonical production hostname;
- approved consent behavior implemented;
- preview and internal testing excluded;
- application and commercial submission events require backend success;
- no PII appears in URLs or analytics parameters; and
- launch deployment is annotated in the reporting record.

### Cloudflare Web Analytics

Cloudflare Web Analytics may be enabled as a privacy-conscious aggregate and operational complement to GA4 after review. It does not replace Search Console, Bing Webmaster Tools, ATS/CRM outcomes, or the GA4 event contract.

### Deployment verification

Test:

- production page view;
- preview page view excluded from production property;
- CTA events;
- job context;
- application success;
- network success;
- commercial success;
- consent states;
- query-string redaction; and
- duplicate prevention after refresh.

---

## 32. Search Platform Activation

### Google Search Console

- verify the domain property through business-controlled DNS;
- retain verification records during DNS migration;
- submit the production sitemap only after launch QA;
- inspect homepage, careers hub, openings index, active job, and representative commercial URLs;
- monitor indexing, Core Web Vitals, structured data, security, and manual actions; and
- do not verify or submit preview hosts as public properties unless needed for a specific diagnostic purpose.

### Bing Webmaster Tools

- verify the production domain;
- submit the production sitemap;
- preserve verification through DNS changes;
- configure IndexNow only through an approved static or serverless implementation;
- monitor crawl, index, search, and AI Performance data where available; and
- do not submit preview URLs.

### IndexNow deployment

If implemented, the key must be handled according to Bing/IndexNow requirements, and submissions should occur only for canonical production URL changes. Do not send draft, preview, noindex, or test URLs.

---

## 33. Local Profile Cutover

After the canonical domain is live and verified:

- update the eligible Google Business Profile website link;
- update Apple Business Connect;
- update Bing Places or connected Microsoft business records where applicable;
- update recruitment profiles and employer pages;
- update verified directories and social profiles;
- preserve approved UTM tracking where platform fields support it; and
- verify the displayed destination and redirect count.

Do not change business name, address, phone, categories, or service areas as part of a technical domain cutover unless those fields have separate business approval.

---

## 34. Deployment Gates

### Gate 1: Build integrity

- clean install succeeds;
- type checking succeeds;
- lint succeeds;
- tests succeed;
- static export succeeds; and
- output inspection succeeds.

### Gate 2: Business truth

- business identity is approved;
- active jobs are verified;
- locations and markets are classified correctly;
- service claims are approved;
- contact routes are accurate; and
- no placeholder remains.

### Gate 3: Search integrity

- canonicals are correct;
- indexation states are intentional;
- sitemap is valid;
- robots behavior is correct;
- schema validates;
- redirects are complete; and
- preview domains are protected.

### Gate 4: Conversion integrity

- forms are accessible;
- production endpoint is correct;
- Turnstile validates server-side;
- ATS/CRM routing is correct;
- confirmation follows accepted processing;
- analytics events are correct; and
- PII is absent from analytics and broad logs.

### Gate 5: Operational readiness

- owners are available;
- DNS plan is approved;
- monitoring is active;
- rollback is ready;
- current-site backup or restore path is verified; and
- stakeholder launch approval is recorded.

No single successful Cloudflare build satisfies all launch gates.

---

## 35. Production Deployment Procedure

1. Freeze the approved release scope.
2. Confirm all required checks pass on the release commit.
3. Record the commit SHA and planned launch time.
4. Confirm production environment variables and secrets.
5. Merge or push the approved commit to `main` through the approved GitHub workflow.
6. Monitor the Cloudflare build log.
7. Confirm the deployment is marked production.
8. Run automated production smoke tests.
9. Run manual priority-route tests.
10. Test application, installer-network, and commercial flows separately.
11. Confirm analytics and consent behavior.
12. Validate headers, redirects, robots, sitemap, canonicals, and JSON-LD.
13. Confirm uptime and alerting.
14. Obtain business acceptance.
15. Record the production deployment ID, URL, commit, time, and verifier.

For an ordinary post-launch release, domain cutover steps are omitted but all relevant application, search, and operational checks remain.

---

## 36. Production Smoke Test

### Priority routes

- `/`;
- `/careers/`;
- `/careers/openings/`;
- one active job;
- one role page;
- one recruiting-market page if published;
- `/careers/installer-requirements/`;
- `/careers/installer-network/`;
- `/services/`;
- one approved service page;
- `/contact/`;
- privacy and applicant privacy pages;
- `/robots.txt`;
- `/sitemap.xml` or sitemap index; and
- a nonexistent URL.

### For each representative HTML route

- response status is correct;
- HTTPS is used;
- canonical host is correct;
- title and description are correct;
- canonical tag is correct;
- robots directive is correct;
- primary content appears in HTML;
- navigation works;
- images and fonts load;
- JSON-LD is valid where applicable;
- forms and CTAs point to the correct journey; and
- no console or network error blocks use.

### Form smoke test

Use clearly marked test records and remove them according to the approved process.

- active-job application reaches only the recruiting test/approved route;
- installer-network registration remains separate;
- commercial inquiry reaches only the commercial pipeline;
- confirmation pages remain noindex;
- analytics events fire once after accepted success; and
- no submitted value appears in the URL, GA4 payload, or general log.

---

## 37. Monitoring After Deployment

### First 30 minutes

- availability;
- DNS resolution;
- TLS certificate;
- canonical and alternate hosts;
- highest-traffic routes;
- forms;
- errors;
- redirects;
- analytics collection; and
- third-party services.

### First 24 hours

- Cloudflare traffic and error patterns;
- 404s;
- submission success and failures;
- ATS/CRM record creation;
- analytics/backend reconciliation;
- sitemap fetch;
- search inspection;
- email and DNS services;
- pages.dev discovery; and
- user-reported issues.

### First 7 days

- crawling and indexing;
- job schema processing;
- redirect demand;
- Core Web Vitals lab and early field signals;
- application completion and error rate;
- traffic-source classification;
- spam and Turnstile failures;
- deployment stability; and
- content or job corrections.

### Ongoing

- uptime checks;
- form endpoint health;
- ATS/CRM delivery failures;
- certificate and domain status;
- build failures;
- security events;
- crawl/index errors;
- active-job expiry;
- access review; and
- dependency/platform changes.

---

## 38. Cloudflare Deployment Records

Maintain a deployment record containing:

- deployment date and time;
- environment;
- Cloudflare deployment ID;
- deployment URL;
- Git commit SHA;
- branch;
- change summary;
- build result;
- tests performed;
- approver;
- verifier;
- form test result;
- search validation result;
- rollback target;
- incident or issue links; and
- final status.

The record may live in GitHub releases, deployment notes, an operations system, or another approved location. Do not include applicant data or secrets.

---

## 39. Rollback Strategy

### Rollback triggers

Rollback may be appropriate when a release causes:

- site-wide outage;
- broken priority navigation;
- inaccessible or unusable applications;
- applications sent to the wrong system;
- false active or closed job status;
- exposed secret or private data;
- severe metadata, canonical, robots, sitemap, or schema failure;
- widespread broken assets;
- destructive redirect loop;
- production analytics contamination that cannot be isolated quickly; or
- material legal or business inaccuracy.

### Cloudflare rollback

Cloudflare Pages supports rolling production back to a previous successful production deployment through the deployment interface, subject to current platform behavior.

### Rollback procedure

1. Confirm the incident and affected scope.
2. Stop further releases.
3. Identify the last known good production deployment and commit.
4. Roll back through Cloudflare if this safely restores service.
5. Verify the canonical domain, priority routes, forms, headers, redirects, and analytics.
6. Correct or revert the repository so Git matches the intended production state.
7. Deploy the repository-backed fix.
8. verify downstream ATS/CRM and candidate data handling.
9. Notify required business and technical owners.
10. Record the incident, impact, timeline, and preventive action.

### Important limitation

A deployment rollback may not reverse:

- DNS changes;
- Cloudflare zone-level Redirect Rules;
- nameserver changes;
- external ATS/CRM records;
- sent emails or SMS;
- environment-variable changes;
- Worker deployments;
- third-party profile updates; or
- search engine crawling.

Each external change requires its own rollback step.

---

## 40. DNS Rollback

### Requirements

- Preserve the previous DNS values before cutover.
- Keep the previous host available for the approved observation period.
- Know whether the prior certificate and origin remain valid.
- Avoid simultaneous unrelated DNS changes.
- Define who can authorize rollback.

### Procedure

1. Confirm the problem is caused by DNS or the new origin.
2. Restore the explicit validated previous record values.
3. Confirm resolution from multiple recursive resolvers or networks.
4. Verify the old origin and certificate.
5. Confirm email and other records remain intact.
6. Keep the new deployment available for diagnosis without public routing.
7. Record exact changes and propagation behavior.

DNS rollback is not instantaneous because recursive resolvers may retain earlier answers until TTL expiration.

---

## 41. Incident Severity

| Severity | Example | Target response |
|---|---|---|
| Critical | Applicant data exposure, wrong ATS routing, full outage, secret exposure | Immediate containment and owner notification |
| High | Applications unavailable, active jobs wrong, redirect loop, canonical/indexing failure across site | Same-day correction or rollback |
| Medium | Broken secondary page, isolated asset failure, incorrect noncritical event | Prioritized fix in normal release flow |
| Low | Cosmetic issue without usability or business impact | Backlog and scheduled correction |

Exact response targets and notification obligations must be approved by the business and any legal/privacy reviewer.

---

## 42. Incident Playbooks

### Failed production build

- confirm old production remains available;
- inspect the first fatal error;
- reproduce with the same Node version and lockfile;
- fix through Git;
- rerun required checks; and
- redeploy.

### Form outage

- confirm site availability separately;
- test backend health and Turnstile;
- stop false success confirmations;
- display the approved fallback contact method if needed;
- alert recruiting or commercial owner;
- reconcile missed or queued submissions; and
- verify before closing the incident.

### Wrong job published

- disable the application path;
- remove job schema and sitemap entry;
- deploy the corrected job registry;
- update recruitment platforms;
- review received applications and communications; and
- document the source-of-truth failure.

### Preview indexed

- add or repair `X-Robots-Tag`;
- remove public links;
- restrict access where appropriate;
- use applicable search removal tools only as a temporary visibility measure;
- preserve production canonicals; and
- audit all preview host patterns.

### Secret exposure

- revoke and rotate immediately;
- inspect access and logs;
- remove the secret from current files and Git history through an approved process where necessary;
- redeploy affected services;
- assess applicant/customer impact;
- notify required parties; and
- add prevention controls.

---

## 43. Access Control

### Recommended Cloudflare roles

| Role | Access |
|---|---|
| Business owner | Account recovery, billing, zone, Pages oversight |
| Technical owner | Pages, DNS, deployment, redirects, headers, logs as required |
| Developer | Pages deployment/configuration necessary for work; limited DNS |
| Content/recruiting reviewer | Preview access; no Cloudflare administrative access by default |
| External vendor | Time-limited least privilege |

### Review events

Review access:

- quarterly;
- after employee or vendor changes;
- after account recovery;
- after an incident;
- before and after launch; and
- when domain or project ownership changes.

### Audit items

- active account members;
- roles;
- API tokens;
- GitHub App scope;
- deploy hooks;
- Access policies;
- Pages and Worker bindings;
- DNS edit permissions;
- account recovery methods; and
- service accounts.

---

## 44. Deploy Hooks and Tokens

### Default position

Do not create deploy hooks until an approved external system needs to trigger a rebuild.

Potential future uses:

- ATS job-open or job-close webhook;
- headless CMS publication;
- scheduled data refresh; and
- approved operations workflow.

### Requirements

- treat hook URLs as secrets;
- scope them to the correct project and branch;
- validate the upstream event before triggering where an integration layer exists;
- rate-limit calls;
- log trigger source safely;
- prevent drafts from triggering production;
- prevent duplicate rapid builds;
- alert on repeated failures; and
- rotate or delete unused hooks.

An exposed deploy hook can create cost, noise, or unwanted deployments even if it cannot edit source code.

---

## 45. Web Analytics and Logs

### Cloudflare Web Analytics

If enabled:

- associate it with the canonical production domain;
- document whether the beacon is injected or added through the site;
- avoid duplicate beacon installation;
- review privacy disclosures;
- exclude or label previews; and
- use it as a supplementary traffic source, not the ATS/CRM outcome system.

### Cloudflare logs and metrics

Depending on plan and enabled products, use available metrics for:

- requests;
- bandwidth;
- status codes;
- cache behavior;
- security activity;
- Pages builds;
- Functions or Workers;
- form endpoint errors; and
- geographic aggregate patterns.

### Privacy rules

- do not log request bodies containing applications or inquiries;
- redact authorization headers and cookies;
- restrict raw logs;
- set retention intentionally;
- do not export applicant data to broad dashboards; and
- review third-party log destinations.

---

## 46. Cost and Limit Review

Before launch and quarterly, review current Cloudflare plan limits and expected usage for:

- Pages builds;
- concurrent builds;
- files and asset sizes;
- preview deployments;
- `_redirects` rules;
- `_headers` rules;
- Workers requests and CPU time if used;
- Turnstile;
- log retention or export;
- Web Analytics;
- image services;
- KV, R2, Queues, or Durable Objects if introduced; and
- support level.

Do not design critical form reliability around an unverified free-tier limit. Record expected traffic, submission volume, build frequency, asset volume, and recovery requirements.

---

## 47. Production Change Types

| Change type | Examples | Required validation |
|---|---|---|
| Content-only | Approved paragraph, FAQ, image alt text | Content, links, layout, SEO checks |
| Job lifecycle | Open, update, pause, close | Recruiting approval, schema, sitemap, form, platform sync |
| Visual | Component style, spacing, image | Responsive, accessibility, performance, screenshots |
| Routing | URL, redirect, navigation | Redirect, canonical, sitemap, links, 404 tests |
| Integration | Form, ATS, CRM, analytics | Security, privacy, test records, failure modes |
| Infrastructure | DNS, domain, headers, build settings | Full deployment plan and rollback |
| Dependency | Framework or package update | Clean install, full checks, build, preview regression |
| Emergency | Outage or high-risk correction | Minimum safe change, immediate verification, incident review |

The required reviewer and launch window should scale with impact.

---

## 48. Routine Release Checklist

### Before merge

- [ ] Scope and owner are clear.
- [ ] Business facts are approved.
- [ ] Job state is verified if affected.
- [ ] Required GitHub checks pass.
- [ ] Preview is reviewed.
- [ ] Accessibility impact is reviewed.
- [ ] SEO/indexation impact is reviewed.
- [ ] Analytics impact is reviewed.
- [ ] Form/data impact is reviewed.
- [ ] Rollback target is known.

### After production deployment

- [ ] Correct commit is live.
- [ ] deployment completed successfully.
- [ ] affected routes return expected status.
- [ ] canonical, robots, schema, and sitemap behavior remain correct.
- [ ] affected conversion routes work.
- [ ] no preview/test configuration is present.
- [ ] monitoring shows no regression.
- [ ] deployment record is updated.

---

## 49. Full Launch Checklist

### Ownership and accounts

- [ ] Business-controlled Cloudflare account confirmed.
- [ ] Primary and backup administrators confirmed.
- [ ] Multifactor authentication enabled.
- [ ] GitHub App limited to required repository.
- [ ] Registrar and DNS access confirmed.
- [ ] Recovery contacts documented privately.

### Repository and build

- [ ] `main` is the production branch.
- [ ] Node version and lockfile are committed.
- [ ] Next.js static export is enabled.
- [ ] `npm run build` produces `out/`.
- [ ] Clean build succeeds locally and in CI.
- [ ] Cloudflare uses correct root, command, and output.
- [ ] Drafts, blocked records, and test jobs are absent.
- [ ] No secrets or PII appear in the artifact or logs.

### Preview

- [ ] Pull-request preview works.
- [ ] Preview is noindex at the response level.
- [ ] Branch and immutable preview host patterns are tested.
- [ ] Preview uses test forms and integrations.
- [ ] Preview does not send production analytics.
- [ ] Sensitive previews use access control when required.

### Domain and DNS

- [ ] Canonical host decision approved.
- [ ] Existing DNS inventory completed.
- [ ] Mail and verification records preserved.
- [ ] Custom domain added through Pages.
- [ ] TLS certificate active.
- [ ] HTTPS works.
- [ ] Alternate host redirects once.
- [ ] Production Pages hostname is noindex or redirected as approved.
- [ ] DNS rollback values are recorded.

### Redirects and headers

- [ ] `_redirects` exists in final output.
- [ ] Legacy URL inventory is mapped.
- [ ] No redirect chains or loops exist.
- [ ] Trailing-slash policy is consistent.
- [ ] `_headers` exists in final output.
- [ ] Preview noindex header works.
- [ ] Security headers are tested.
- [ ] CSP rollout decision is documented.
- [ ] HSTS decision is documented.
- [ ] asset caching is verified.

### Search

- [ ] Production robots file is correct.
- [ ] Sitemap contains only canonical indexable URLs.
- [ ] Active jobs contain valid matching JobPosting schema.
- [ ] Closed jobs have no active schema.
- [ ] Search Console domain verification works.
- [ ] Bing verification works.
- [ ] Production sitemap is submitted after QA.
- [ ] Preview URLs are not submitted.

### Forms and data

- [ ] Production form endpoints are approved.
- [ ] Preview endpoints are separate.
- [ ] Turnstile is verified server-side.
- [ ] ATS/CRM field mapping is tested.
- [ ] Recruitment and commercial routes are separate.
- [ ] duplicate protection works.
- [ ] failure does not display success.
- [ ] no PII enters analytics or broad logs.
- [ ] secure file handling is approved or uploads are excluded.
- [ ] retention and access controls are approved.

### Measurement and operations

- [ ] GA4 production collection is validated.
- [ ] Preview traffic is excluded.
- [ ] server-confirmed conversion events work.
- [ ] Cloudflare monitoring is active.
- [ ] uptime and form alerts reach named owners.
- [ ] ATS/CRM failure alert works.
- [ ] deployment record process exists.
- [ ] rollback is understood and target identified.
- [ ] launch support owners are available.

---

## 50. Open Decisions

1. Is the apex or `www` host canonical?
2. Is the domain currently using Cloudflare nameservers?
3. Who owns the Cloudflare account and zone?
4. Who has registrar access?
5. What Pages project name will be used?
6. Which Node version will be pinned?
7. Will GitHub Actions run the full gate before Cloudflare builds?
8. Which branches receive previews?
9. Will stakeholder previews require Cloudflare Access?
10. Will the production `pages.dev` hostname be redirected or kept noindex?
11. What is the verified legacy URL inventory?
12. Which form backend will be deployed?
13. Which ATS/recruiting CRM receives applications?
14. Which commercial CRM receives inquiries?
15. Will resume uploads exist at launch?
16. Which Turnstile account and hostname configuration will be used?
17. Which analytics and consent tools are approved?
18. Will Cloudflare Web Analytics be enabled?
19. Will a CSP be enforced at launch or rolled out after report-only testing?
20. What HSTS rollout is approved?
21. Which monitoring service and alert destinations will be used?
22. What is the launch and rollback window?
23. How long will the prior host remain available?
24. Who may authorize a production rollback?
25. What Cloudflare plan and support level are appropriate?

These decisions must be resolved before the affected production step. Do not substitute unverified assumptions in DNS, form, security, or data-handling configuration.

---

## 51. Definition of Done

Cloudflare Pages deployment is complete when:

- the production Pages project is business-controlled;
- GitHub access is limited to the required repository;
- `main` is the only production branch;
- the pinned Node and lockfile build reproducibly;
- Cloudflare publishes the Next.js `out` directory;
- preview deployments are usable and non-indexable;
- preview forms and analytics are isolated from production;
- the approved custom domain serves valid HTTPS;
- the alternate host resolves through one permanent redirect;
- the production `pages.dev` hostname is handled intentionally;
- legacy redirects and trailing-slash behavior pass tests;
- security and cache headers work on static responses;
- Function or Worker responses set their own required headers;
- production forms reach the correct ATS/CRM only after server validation;
- no secret or PII appears in the static artifact, Git, analytics, or broad logs;
- sitemap, robots, canonicals, and schema are correct;
- Search Console and Bing verification are preserved;
- monitoring and alerting are operational;
- deployment and DNS changes are recorded;
- a known-good rollback target exists; and
- launch acceptance is recorded by the authorized business and technical owners.

---

## 52. Authoritative References

Confirm all settings against current Cloudflare and framework documentation:

- [Cloudflare Pages: static Next.js deployment](https://developers.cloudflare.com/pages/framework-guides/nextjs/deploy-a-static-nextjs-site/)
- [Cloudflare Pages GitHub integration](https://developers.cloudflare.com/pages/configuration/git-integration/github-integration/)
- [Cloudflare Pages build configuration](https://developers.cloudflare.com/pages/configuration/build-configuration/)
- [Cloudflare Pages branch deployment controls](https://developers.cloudflare.com/pages/configuration/branch-build-controls/)
- [Cloudflare Pages preview deployments](https://developers.cloudflare.com/pages/configuration/preview-deployments/)
- [Cloudflare Pages custom domains](https://developers.cloudflare.com/pages/configuration/custom-domains/)
- [Cloudflare Pages redirects](https://developers.cloudflare.com/pages/configuration/redirects/)
- [Cloudflare Pages headers](https://developers.cloudflare.com/pages/configuration/headers/)
- [Cloudflare Pages rollbacks](https://developers.cloudflare.com/pages/configuration/rollbacks/)
- [Cloudflare Pages build caching](https://developers.cloudflare.com/pages/configuration/build-caching/)
- [Cloudflare Turnstile server-side validation](https://developers.cloudflare.com/turnstile/get-started/server-side-validation/)
- [Next.js static exports](https://nextjs.org/docs/app/guides/static-exports)
- [GitHub protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches)

Current platform documentation and tested production behavior take precedence over examples in this draft.

---

## 53. Related Project Documents

- `00-project-overview.md`
- `01-business-source-of-truth.md`
- `07-site-architecture.md`
- `08-url-strategy.md`
- `10-on-page-seo-standards.md`
- `11-local-seo-plan.md`
- `12-aeo-geo-llm-optimization.md`
- `13-schema-markup-plan.md`
- `14-conversion-strategy.md`
- `15-analytics-and-measurement.md`
- `16-technical-architecture.md`
- `18-github-workflow.md`
- `25-quality-assurance-checklist.md`
- `26-launch-checklist.md`

The technical architecture defines what is built. This document defines how the static output is deployed and operated on Cloudflare Pages. GitHub workflow, QA, and launch documents may add procedural detail but must preserve this environment, security, indexation, and rollback model.

---

## 54. Maintenance Standard

Update this document when:

- Cloudflare Pages or static Next.js guidance changes;
- the Cloudflare account, zone, project, plan, or ownership changes;
- the production branch or repository changes;
- the build command, output directory, Node version, or package manager changes;
- the canonical host or DNS architecture changes;
- a Worker, Pages Function, CMS, ATS, CRM, queue, or storage service is added;
- preview access or indexation controls change;
- redirect, header, CSP, HSTS, or cache rules change;
- analytics or consent configuration changes;
- a platform limit affects the architecture;
- a deployment or DNS incident reveals a control gap; or
- the rollback and monitoring process changes.

Record material changes with the date, owner, reason, affected environments, validation evidence, and rollback impact.
